# Rule: Writing style

- Never use an em dash ("—") in commit messages, PR descriptions, code comments, or any other text written into this repo. Use a comma, a period, or "and" instead.
- Keep code comments to a single line, and only when the *why* is non-obvious (a workaround, an invariant, a constraint), never a restatement of what the code already says.
