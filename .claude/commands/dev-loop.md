---
description: Run the project's typecheck + test + build loop and fix whatever fails.
---

Run the `dev-loop` skill now against the current working tree. Fix anything that fails and report a one-line summary when everything is green.
